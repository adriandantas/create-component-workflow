apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: create-component-automation-
spec:
  workflowTemplateRef:
    name: create-component-automation
  workflowMetadata:
    labels:
      automationType: execution
